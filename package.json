{
  "name": "pumpsync-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port 8766",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
